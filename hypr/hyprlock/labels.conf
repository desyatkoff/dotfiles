label {
    monitor =

    text = cmd[update:1000] date +"%H:%M:%S"

    color = rgb(cdd6f4)

    font_size = 80
    font_family = Adwaita Sans

    position = 0, 128

    halign = center
    valign = center
}

label {
    monitor =

    text = cmd[update:1000] date +"%A, %d %B %Y"

    color = rgb(a6adc8)

    font_size = 20
    font_family = Adwaita Sans

    position = 0, 64

    halign = center
    valign = center
}
